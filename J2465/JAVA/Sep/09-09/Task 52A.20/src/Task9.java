import java.util.Arrays;

public class Task9 {
    public static int[] run(int size, int element) {
        int[] numbers = new int[size];
        Arrays.fill(numbers, element);
        return numbers;
    }
}
